name: Metrics
on:
  schedule: [{cron: "0 */12 * * *"}]
  workflow_dispatch:
  push: {branches: ["master", "main"]}

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      # 1. Header 左侧：增加成就插件，让内容变丰富
      - name: Header Left - Scholar & Languages
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: header-left.svg
          base: ""
          plugin_scholar: yes
          plugin_scholar_userid: pD1NOyUAAAAJ
          plugin_languages: yes
          plugin_languages_sections: most-used
          plugin_achievements: yes # 增加成就奖章，填补垂直空间
          plugin_achievements_display: compact

      # 2. Header 右侧：减小 calendar 的缩放
      - name: Header Right - Activity & Calendar
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: header-right.svg
          base: header, activity
          plugin_isocalendar: yes
          plugin_isocalendar_duration: half-year
          config_padding: 0, 0, 0, 0 # 确保右侧没有额外间距

      # 3. 循环生成 Repo 卡片
      - name: Repo - Aether
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: repo-aether.svg
          template: repository
          repo: Aether
          repo_user: OpenRobotLab  # 修正：从 user 改为 repo_user

      - name: Repo - DeepVerse
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: repo-deepverse.svg
          template: repository
          repo: DeepVerse
          repo_user: SOTAMak1r    # 修正：从 user 改为 repo_user

      - name: Repo - VQ-VLA
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: repo-vqvla.svg
          template: repository
          repo: VQ-VLA
          repo_user: xiaoxiao0406 # 修正：从 user 改为 repo_user

      - name: Repo - SPA
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: repo-spa.svg
          template: repository
          user: HaoyiZhu
          repo: SPA

      - name: Repo - PointCloudMatters
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: repo-pcm.svg
          template: repository
          user: HaoyiZhu
          repo: PointCloudMatters

      - name: Repo - rh20t_api
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: repo-rh20t.svg
          template: repository
          repo: rh20t_api
          repo_user: rh20t        # 修正：从 user 改为 repo_user

      - name: Repo - MineDojo
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: repo-minedojo.svg
          template: repository
          repo: MineDojo
          repo_user: MineDojo     # 修正：从 user 改为 repo_user

      - name: Repo - Pi3
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: repo-pi3.svg
          template: repository
          repo: Pi3
          repo_user: yyfz           # 修正：从 user 改为 repo_user

      - name: Repo - PonderV2
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: repo-ponderv2.svg
          template: repository
          repo: PonderV2
          repo_user: OpenGVLab     # 修正：从 user 改为 repo_user

      - name: Repo - AlphaPose
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: repo-alphapose.svg
          template: repository
          repo: AlphaPose
          repo_user: MVIG-SJTU     # 修正：从 user 改为 repo_user

      - name: Repo - XNeRF
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: repo-xnerf.svg
          template: repository
          user: HaoyiZhu
          repo: XNeRF
